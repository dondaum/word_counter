<p id="notice"><%= notice %></p>

<h1>Willkommen auf dieser kleinen Testseite</h1>
<p> Mit Hife dieser Seite kann man Texte analysieren. Ein</P>
<table>
  <thead>
    <tr>
      <th>Text</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @texts.each do |text| %>
      <tr>
        <td><%= text.text %></td>
        <td><%= link_to 'Details', text %></td>
        <td><%= link_to 'Bearbeiten', edit_text_path(text) %></td>
        <td><%= link_to 'Löschen', text, method: :delete, data: { confirm: 'Sind Sie sicher?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neuen Text speichern', new_text_path %>
